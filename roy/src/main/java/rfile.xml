<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="rfilens">
	<!-- <select id="select" parameterType="int" resultType="rfile">
		select * from file where f_num=#{f_num} 
	</select>
	<insert id="insert" parameterType="rfile">
		insert into file values (#{f_num},#{f_name},#{f_path})
	</insert> -->
</mapper>